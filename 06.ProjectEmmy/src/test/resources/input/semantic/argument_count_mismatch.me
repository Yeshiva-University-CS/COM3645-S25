# Test for ARGUMENT_COUNT_MISMATCH error
# Calling a function with too few arguments

let add a b = a + b;
print add(5);  # Error: Argument count mismatch

# Calling a function with too many arguments
let greet name = "Hello, " + name;
print greet("Alice", "Bob", "Charlie");  # Error: Argument count mismatch
