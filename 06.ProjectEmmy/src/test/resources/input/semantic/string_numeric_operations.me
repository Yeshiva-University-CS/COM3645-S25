# Test for string/numeric operations compatibility errors

# String concatenation is valid
var greeting = "Hello, ";
var name = "World";
var message = greeting + name;  # Valid: strings can be concatenated

# Subtraction with strings is not allowed
var str1 = "Hello";
var str2 = "World";
var invalid = str1 - str2;  # Error: Subtraction operands must be numeric

# Testing that type compatibility is checked
var num = 42;
var str = "text";
print num + " " + str;  # Valid
print str + " " + num;  # Valid
print num - str;  # Error: Subtraction operands must be compatible
print str - num;  # Error: Subtraction operands must be compatible

var rnum = 42.3;
print rnum + " " + str;  # Valid
print str + " " + rnum;  # Valid
print rnum - str;  # Error: Subtraction operands must be compatible
print str - rnum;  # Error: Subtraction operands must be compatible

# Testing numeric operations on strings
var a = "10";
var b = "20";
var product = a * b;  # Error: Multiply/Divide operands must be numeric

# Division with strings
var quotient = a / b;  # Error: Multiply/Divide operands must be numeric

